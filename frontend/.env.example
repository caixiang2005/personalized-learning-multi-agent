# ─────────────────────────────────────────────────────────────
# 为何仓库里只有 .env.example，没有 .env？
# Git 不提交每人本地的 .env / .env.development（可能含密钥、个人开关）。
# 队友克隆后请复制本文件：
#   copy .env.example .env.development   （Windows）
#   cp .env.example .env.development     （macOS/Linux）
# 然后执行 npm run dev（Vite 会自动读取 .env.development）。
# ─────────────────────────────────────────────────────────────

VITE_API_BASE=/api

# 开发代理（vite.config.ts，不必写在 env 里）：
#   /api/user/*  → user-service  http://127.0.0.1:8001
#   /api/agent/* → agent-service http://127.0.0.1:8003

# 后端未就绪、仅看 UI 时取消下一行注释：
# VITE_USE_MOCK=true

# 访客 AI 强制 Mock（不请求 agent-service:8003）：
# VITE_GUEST_CHAT_MOCK=1
