# learn-service 环境变量（复制为 config/.env）
# 若未创建本文件，启动时会自动读取 user-service/config/.env 中的 DATABASE_URL
DATABASE_URL=postgresql+psycopg2://user:password@127.0.0.1:5432/project_db
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=1
SERVICE_PORT=8002
