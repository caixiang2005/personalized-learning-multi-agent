# agent-service 运行依赖
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
# Windows 若报 WinError 206（路径过长），可先：
#   subst P: "%CD%\.venv"
#   P:\Scripts\python.exe -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#   subst P: /d

# ── Web / API ──
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9
pydantic>=2.0,<3.0
pydantic-settings>=2.0
python-dotenv>=1.0
httpx>=0.27
PyYAML>=6.0

# ── LLM ──
openai>=1.40.0
tiktoken>=0.7

# ── 缓存 / 数据库（RAG） ──
redis>=5.0
asyncpg>=0.29
psycopg2-binary>=2.9
pgvector>=0.3

# ── 向量检索（登录用户 RAG 聊天必需；会拉取 torch，体积较大） ──
sentence-transformers>=3.0.0,<4.0
torch>=2.0.0

# ── 图像（OCR / 上传预处理） ──
Pillow>=10.0.0

# ── 可选工具（当前仅 tools/get_weather 使用；不装也可启动主服务） ──
# langchain>=0.3.0
# langchain-openai>=0.2.0

# ── 拍照搜题 OCR（可选；未装时 scan 会提示，画像/路径/辅导仍可用） ──
# 依赖较重（torchvision / onnx 等）。Windows 路径过长时建议单独装：
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple "cnocr>=2.3.0"
cnocr>=2.3.0
