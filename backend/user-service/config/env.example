# Copy to config/.env
DATABASE_URL=postgresql+psycopg2://team_user:PASSWORD@RDS_HOST:5432/project_db

# Optional overrides (settings.yaml redis/smtp still used if present)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
